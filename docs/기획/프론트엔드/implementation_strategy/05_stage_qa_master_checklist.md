# Stage QA 마스터 체크리스트

> 각 Stage 완료 전 이 체크리스트를 기준으로 수동 QA를 수행한다.
> 기능 성공 여부뿐 아니라 **디자인/UX 품질**과 **실패/복구 품질**까지 확인한다.

---

## F1 체크리스트

> 상태: 완료 (F1 DEVLOG 기준)

### Happy path — 기능

- [x] `/` 진입 후 [지금 시작하기] CTA로 `/upload` 이동 가능
- [x] 유효한 이미지 업로드 후 `/match`로 이동
- [x] sessionStorage에 Top3 데이터 저장 확인
- [x] `/match`에서 Top3 카드 3개 렌더링 (스캐폴딩)
- [x] `/intro` → skip → `/` 이동

### Failure path — 기능

- [x] 비이미지 파일(PDF/txt 등) → 즉시 에러, 서버 호출 없음
- [x] 10MB 초과 파일 → 즉시 에러, 서버 호출 없음
- [x] 얼굴 검출 실패(`FACE_NOT_DETECTED`) → 안내 메시지 표시
- [x] 얼굴 2개 이상(`MULTIPLE_FACES`) → 안내 메시지 표시
- [x] 서버 500 에러 → "잠시 후 다시 시도해 주세요" Toast

### Recovery path

- [x] 실패 후 파일 재선택 → 정상 재시도 가능
- [x] `/match` 세션 유실 → `/upload` 자동 복귀

### 디자인/UX 검증 (F1)

- [ ] DropZone drag over 시 코랄(#f15946) 테두리 표시
- [ ] 업로드 중 Spinner + "포켓몬을 찾는 중..." 텍스트
- [ ] 인트로 배경 검정, 포켓몬 이미지 serebii.net Pokemon GO 아트
- [ ] 랜딩 Hero ghost text (배경 대형 반투명 텍스트) 확인
- [ ] 공개 샘플 섹션 API 실패 시 섹션 숨김 (전체 레이아웃 불깨짐)
- [ ] 모바일 MobileNav 하단 고정 탭바 표시
- [ ] lg 이상에서 MobileNav 숨김

---

## F2 체크리스트

### Happy path — 기능

- [ ] `/match` 포켓몬 카드 선택 → fade out + `/generate/[id]` 진입
- [ ] `/generate` 진입 즉시 퍼레이드 애니메이션 시작 (클릭 없이)
- [ ] 상태 텍스트: 0s/10s/25s 순차 전환 확인
- [ ] 폴링 완료(succeeded) → `/result/[id]` 자동 이동
- [ ] `/result` 카드 등장 애니메이션 0~1.8s 시퀀스 확인
- [ ] 컨페티 0.8s에 발화 (1회만)
- [ ] 이름 클릭 → input 전환 → Enter → 저장 → 텍스트 모드 복귀
- [ ] [공유] 버튼 → Web Share API 또는 링크 복사
- [ ] 비로그인 [광장에 올리기] → 로그인 유도 모달 표시
- [ ] 로그인 완료 → 원래 액션 자동 재실행
- [ ] 회원가입 → 자동 로그인 → `/upload` 이동

### Happy path — 디자인 검증 (F2)

- [ ] `/match` 카드: 타입별 배경 색상 (전기=`#c9a93b`, 불꽃=`#c97c4a` 등)
- [ ] `/match` 카드: 다이아몬드 패턴 (repeating-linear-gradient 45deg) 표시
- [ ] `/match` 카드: serebii.net Pokemon GO 3D 아트 이미지 표시
- [ ] `/match` 카드: 이미지 head 바운스 애니메이션 (2s loop)
- [ ] `/match` 카드: 라디얼 글로우 효과
- [ ] `/match` 카드: 우측 패널 Lato 폰트, letter-spacing: 0.2em
- [ ] `/match` 카드: 유사도 프로그레스바 (타입 색상)
- [ ] `/match` 1위 카드: scale(1.05) + "BEST MATCH" 뱃지
- [ ] `/generate` 배경: `#1D1F20` 다크
- [ ] `/generate` PocketmanLogo opacity 0.8 표시
- [ ] `/generate` 퍼레이드: 포켓몬 스프라이트 좌→우 흐름
- [ ] `/result` 카드: 크리처 타입 색상 배경 + 다이아몬드 패턴
- [ ] `/result` 컨페티: 코랄+틸+골드 색상 조합
- [ ] `/result` 영상: 클릭 전 ▶ 아이콘 오버레이, 클릭 시 재생
- [ ] 데스크톱 `/login`: 좌우 50/50 분할 레이아웃
- [ ] 데스크톱 `/login`: 포켓몬 3장 부채꼴 배치 (-8deg/+5deg/-3deg)
- [ ] 데스크톱 `/login`: 각 포켓몬 눈 좌우 이동 + 깜빡임 애니메이션
- [ ] 모바일 `/login`: 팔레트 타운 일러스트 상단 55% 차지
- [ ] 모바일 `/login`: 구름 이동 (8s), Pidgey 비행 (6s), 피카츄 꼬리 (0.5s), 풀 흔들림 (2s)
- [ ] 모바일 `/login`: 하단 폼 카드 슬라이드업 애니메이션

### Failure path

- [ ] 생성 실패(`veo_job.status === 'failed'`) → 에러 안내 + [다시 시도] + [처음으로]
- [ ] 폴링 타임아웃(75초/25회) → "시간이 너무 걸려요" + [처음으로]
- [ ] `veo_job` null → `image_url` fallback으로 결과 표시 (에러 아님)
- [ ] 로그인 실패 → "이메일 또는 비밀번호가 올바르지 않아요" 인라인 에러
- [ ] 이메일 중복 가입(409) → "이미 사용 중인 이메일이에요" 인라인 에러
- [ ] OAuth 창 닫음(취소) → 로그인 페이지 유지 + "취소됐어요" Toast
- [ ] serebii.net 이미지 404 → 포켓볼 placeholder 표시

### Recovery path

- [ ] 생성 실패 후 [다시 시도] → 폴링 재시작 정상
- [ ] 로그인 완료 후 원래 액션 자동 재실행 (`?next=` 복귀)
- [ ] 이름 편집 중 네트워크 실패 → 원래 이름 복원 + Toast 에러

---

## F3 체크리스트

### Happy path — 기능

- [ ] 크리처 공개 설정 → `/creatures/[id]` 공개 접근 가능
- [ ] `/creatures/[id]` ❤️ 좋아요 → 카운트 증가 확인
- [ ] `/creatures/[id]` 댓글 작성 → 최신순 정상 정렬
- [ ] `/creatures/[id]` 본인 댓글 삭제 → 목록에서 제거
- [ ] `/my` 내 크리처 탭 → 그리드 정상 표시
- [ ] `/my` 좋아요한 크리처 탭 전환 → 데이터 정상 표시
- [ ] `/my/edit` 닉네임 변경 + 중복 검사 통과 → 저장 성공
- [ ] `/my/edit` 카드 미리보기 편집 내용 즉시 반영 확인
- [ ] 다크 모드 토글 → 즉시 전환 + 새로고침 후 유지
- [ ] 글자 크기 슬라이더 → 즉시 미리보기 적용

- [ ] `/plaza` 진입 → 내 크리처 스프라이트 맵에 표시
- [ ] WASD / 방향키로 이동 → 카메라 추적
- [ ] 모바일 D-pad → 이동 정상
- [ ] 이동 불가 타일(분수/나무) 충돌 정상
- [ ] [채팅] 버튼 → 말풍선 전송 → 3초 후 fadeOut
- [ ] 타 플레이어 클릭 → davidkpiano 카드 모달 팝업
- [ ] DM 요청 → 상대방 수락 → 양쪽 채팅창 오픈
- [ ] DM 메시지 주고받기 → 실시간 수신

### Happy path — 디자인 검증 (F3)

- [ ] `/creatures/[id]` 카드: davidkpiano 레이아웃 (match/result와 동일)
- [ ] `/creatures/[id]` 영상: 클릭 전 ▶ 아이콘, 클릭 시 재생
- [ ] 내 크리처: [이름 편집][공개/비공개][삭제][공유] 버튼 표시
- [ ] 타인 크리처: [❤️ 좋아요][📤 공유][광장에서 찾기] 버튼 표시
- [ ] "광장에서 찾기": 온라인 → 활성화 / 오프라인 → 회색 disabled
- [ ] `/plaza` 맵: 체크무늬 바닥 타일, 중앙 분수
- [ ] 크리처 스프라이트: 원형 마스크, 픽셀 테두리, 닉네임 태그
- [ ] 이동 애니메이션: 2프레임 bounce
- [ ] 정지 애니메이션: idle breathing (scale 1.0↔1.03)
- [ ] 말풍선: 흰 배경 + 픽셀 테두리 + 아래 꼬리
- [ ] DM 채팅창: 우하단 float 위치
- [ ] 크리처 없는 신규 유저: 안내 모달 표시
- [ ] `/my` 아바타: 대표 크리처 원형
- [ ] `/my/edit` 상단 카드 미리보기 (davidkpiano 스타일)
- [ ] 다크 모드: 배경 `#1a1a2e`, 카드 `#16213e` 전환 확인
- [ ] 다크 모드 전환 transition (0.3s ease)

### Failure path

- [ ] 비공개 크리처 외부 링크 접근 → 404
- [ ] 비로그인 댓글 작성 클릭 → 로그인 유도 모달
- [ ] 비로그인 좋아요 클릭 → 로그인 유도 모달
- [ ] 타인 댓글 삭제 시도 → 버튼 없음 (또는 403)
- [ ] 닉네임 중복 → 인라인 에러 + [저장] 버튼 disabled
- [ ] WebSocket 연결 실패 → fallback 안내 (관전 모드)
- [ ] DM 요청 거절 → "거절됐어요" Toast
- [ ] 20자 초과 채팅 입력 → 전송 차단 또는 truncate
- [ ] 회원 탈퇴 2단계 확인 → [취소] → 정상 복귀

### Recovery path

- [ ] WebSocket 끊김 후 재연결 → 타 플레이어 목록 재동기화
- [ ] 댓글 저장 실패 → 입력 내용 유지 + 재시도 가능
- [ ] 프로필 저장 실패 → 입력 상태 유지 + Toast 에러

---

## 공통 비기능 점검 (전 Stage 공통)

### 반응형

- [ ] 모바일 (<640px): 단일 컬럼, MobileNav 표시
- [ ] 태블릿 (640~1024px): 중간 레이아웃
- [ ] 데스크톱 (>1024px): 멀티 컬럼, MobileNav 숨김

### 접근성

- [ ] 주요 버튼 Tab 키 접근 가능
- [ ] 버튼 focus 스타일 표시
- [ ] 이미지 alt 텍스트 설정

### 성능/UX

- [ ] API 로딩 중 Spinner/스켈레톤 표시 (빈 화면 없음)
- [ ] 로딩 상태에서 중복 클릭 방지 (버튼 disabled)
- [ ] 에러 발생 시 재시도 방법 안내 포함

### 브라우저 제약 대응

- [ ] Web Share API 미지원 → 링크 복사 fallback
- [ ] 영상 autoplay 차단 → click-to-play 정상 동작
- [ ] sessionStorage 미지원 → 오류 없이 graceful degradation

---

## 릴리즈 전 공통 확인

- [ ] Stage 범위 밖 기능이 섞여 들어가지 않았는지 확인
- [ ] `04_api_alignment_matrix.md`의 미결 사항이 모두 해소됐는지 확인
- [ ] 오픈 이슈가 다음 Stage 백로그에 정리됐는지 확인
- [ ] `npm run build` 오류 없음
- [ ] `npm run lint` ESLint 0 errors
- [ ] 개발일지 작성 완료
